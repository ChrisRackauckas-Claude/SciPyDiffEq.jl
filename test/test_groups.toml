[Core]
versions = ["lts", "1", "pre"]

# PyCall/Conda has no usable i686 build on GHA; keep the lane for signal but non-blocking.
["Core 32-bit"]
group = "Core"
versions = ["1"]
os = ["ubuntu-latest"]
arch = "x86"

[QA]
versions = ["lts", "1"]
